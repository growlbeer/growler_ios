mutation Login ($input: LoginInput!) {
    login(input: $input) {
        user {
            id
            first_name
            last_name
            email
            auth_token
        }
    }
}
